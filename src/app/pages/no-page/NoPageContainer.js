import { connect } from 'react-redux';
import NoPage from './NoPage';

const mapStateToProps = state => ({

});

const NoPageContainer = connect(mapStateToProps, {})(NoPage);

export default NoPageContainer;