import { connect } from 'react-redux';
import Rescue from './Rescue';

const mapStateToProps = state => ({

});

const RescueContainer = connect(mapStateToProps, {})(Rescue);

export default RescueContainer;