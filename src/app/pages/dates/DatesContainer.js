import { connect } from 'react-redux';
import Dates from './Dates';

const mapStateToProps = state => ({});

const DatesContainer = connect(mapStateToProps, {})(Dates);

export default DatesContainer;
