// Actions


// Initial State
const initialState = {
  isAuth: false
};

// Reducer
export const reducer = (state = initialState, action) => {
  switch(action.type) {

    default:
      return state;
  }
}

export default reducer;