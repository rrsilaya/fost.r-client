// Actions

// Initial State
const initialState = {
  isAuth: true
};

// Reducer
export const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
